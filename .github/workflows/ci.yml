name: CI DevOps


on:
  push: 
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

   
    steps:
      - name: Descargar código
        uses: actions/checkout@v3

      -name: Ejecutar script
       run: echo "Hola Camila DevOps"

